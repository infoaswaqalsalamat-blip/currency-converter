<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>محول العملات الذكي - ميزة التبديل</title>
    <style>
        :root { --primary: #2d3436; --accent: #0984e3; --bg: #dfe6e9; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: var(--bg); display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .card { background: white; padding: 2rem; border-radius: 20px; box-shadow: 0 20px 40px rgba(0,0,0,0.1); width: 100%; max-width: 420px; text-align: center; }
        h2 { color: var(--primary); margin-bottom: 1.5rem; border-bottom: 2px solid #eee; padding-bottom: 10px; }
        .input-group { margin-bottom: 15px; text-align: right; }
        label { font-size: 0.9rem; color: #636e72; font-weight: bold; margin-right: 5px; }
        input, select { width: 100%; padding: 12px; border: 2px solid #eee; border-radius: 12px; font-size: 16px; transition: 0.3s; box-sizing: border-box; }
        input:focus { border-color: var(--accent); outline: none; }
        
        /* تصميم زر التبديل */
        .swap-container { margin: 10px 0; }
        .swap-btn { background: #f1f2f6; border: none; width: 45px; height: 45px; border-radius: 50%; cursor: pointer; font-size: 20px; transition: 0.3s; color: var(--accent); }
        .swap-btn:hover { background: var(--accent); color: white; transform: rotate(180deg); }

        button.convert-btn { width: 100%; padding: 15px; background: var(--accent); color: white; border: none; border-radius: 12px; cursor: pointer; font-size: 18px; font-weight: bold; margin-top: 10px; }
        #result { margin-top: 20px; padding: 20px; background: #f9f9f9; border-radius: 12px; border-right: 5px solid var(--accent); font-size: 1.3rem; font-weight: bold; color: #2d3436; }
        .footer { margin-top: 15px; font-size: 0.7rem; color: #b2bec3; }
    </style>
</head>
<body>

<div class="card">
    <h2>محول العملات العالمي</h2>
    
    <div class="input-group">
        <label>المبلغ</label>
        <input type="number" id="amount" value="1" oninput="convert()">
    </div>

    <div class="input-group">
        <label>من</label>
        <select id="fromCurrency" onchange="convert()">
            <option value="USD">USD - دولار أمريكي</option>
            <option value="SYP" selected>SYP - ليرة سورية</option>
            <option value="SAR">SAR - ريال سعودي</option>
            <option value="TRY">TRY - ليرة تركية</option>
            <option value="EUR">EUR - يورو</option>
            <option value="EGP">EGP - جنيه مصري</option>
        </select>
    </div>

    <div class="swap-container">
        <button class="swap-btn" onclick="swapCurrencies()" title="تبديل العملات">⇅</button>
    </div>

    <div class="input-group">
        <label>إلى</label>
        <select id="toCurrency" onchange="convert()">
            <option value="USD" selected>USD - دولار أمريكي</option>
            <option value="SYP">SYP - ليرة سورية</option>
            <option value="SAR">SAR - ريال سعودي</option>
            <option value="TRY">TRY - ليرة تركية</option>
            <option value="EUR">EUR - يورو</option>
            <option value="EGP">EGP - جنيه مصري</option>
        </select>
    </div>

    <div id="result">1 SYP = ...</div>
    
    <div class="footer">
        بيانات حية من الأسواق العالمية • محدث تلقائياً
    </div>
</div>

<script>
    async function convert() {
        const amount = document.getElementById('amount').value;
        const from = document.getElementById('fromCurrency').value;
        const to = document.getElementById('toCurrency').value;
        const resultDiv = document.getElementById('result');

        if (!amount || amount <= 0) {
            resultDiv.innerHTML = "أدخل مبلغاً";
            return;
        }

        try {
            const response = await fetch(`https://api.exchangerate-api.com/v4/latest/${from}`);
            const data = await response.json();
            const rate = data.rates[to];
            const total = (amount * rate).toLocaleString(undefined, {maximumFractionDigits: 2});
            
            resultDiv.innerHTML = `${amount} ${from} = ${total} ${to}`;
        } catch (error) {
            resultDiv.innerHTML = "خطأ في الاتصال";
        }
    }

    function swapCurrencies() {
        const from = document.getElementById('fromCurrency');
        const to = document.getElementById('toCurrency');
        
        // تبديل القيم
        const temp = from.value;
        from.value = to.value;
        to.value = temp;
        
        // إعادة الحساب بعد التبديل
        convert();
    }

    // التشغيل عند البداية
    window.onload = convert;
</script>

</body>
</html>
